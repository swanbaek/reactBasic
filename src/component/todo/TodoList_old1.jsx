import React from 'react'
import {Stack}  from 'react-bootstrap'
export default function TodoList({todo}) {
  return (
    <Stack>
      <h1>TodoList</h1>



      {/* {todo.map((work, i)=>(
        <h4 key={work.id}>{work.content}</h4>
      ))} */}
    </Stack>
  )
}

